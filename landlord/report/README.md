# CoverageFit Landlord Report Adapter

This directory reserves the shared Executive Report Engine for the future Landlord assessment. Store the completed report under `coveragefit_landlord_report` and use `data-report-type="landlord"` on the report page. The shared engine will populate the cover and executive summary automatically.
