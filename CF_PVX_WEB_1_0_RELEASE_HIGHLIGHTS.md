# Release highlights — v3.20.136

The two products now share one authoritative map of what 408FARMERS may send into CoverageFit and what that information means. The contract prevents route, campaign, occupation, contact, and discovery context from being overinterpreted before the secure native handoff is implemented.

