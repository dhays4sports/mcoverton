import assert from 'node:assert/strict';import fs from 'node:fs';import {buildProducerOpportunityProjection,validateProducerOpportunity} from './server/pvx-producer-opportunity-core.mjs';import {projectUnifiedProducerRecord} from './server/pvx-unified-producer-record-core.mjs';
if(process.env.COVERAGEFIT_REGRESSION!=='1')assert.equal(fs.readFileSync('VERSION','utf8').trim(),'3.20.184');
const checkpoint={checkpointId:'c1',checkpointType:'snapshot_saved',snapshot:{whyNowThread:{headline:'My renewal increased',connection:'My renewal increased. Price matters.',evidenceRefs:[{key:'shoppingReason',exactCustomerWords:'My renewal increased',status:'customer-reported'}]},wantsToImprove:[{label:'Keep price central'}],whatDylanWouldLookAtFirst:[{topicKey:'cost_focused_comparison',label:'Cost-focused comparison'}]},actionReadinessExpressions:[{expressionId:'pvr_ready123',state:'price_dependent',sourceCheckpoint:'snapshot',expressedAt:'2026-08-22T00:00:00Z',status:'active'}],changeScopeExpressions:[],desiredNextActions:[{actionId:'pva_action123',action:'see_if_comparison_is_worthwhile',sourceCheckpoint:'snapshot',selectedAt:'2026-08-22T00:01:00Z',status:'active'}],topicResponses:[],consent:{contact:false},createdAt:'2026-08-22T00:00:00Z'};
const projection=buildProducerOpportunityProjection({checkpoint});assert.equal(validateProducerOpportunity(projection),true);assert.equal(projection.currentActionReadiness.state,'price_dependent');assert.equal(projection.currentChangeScope,'unknown');assert.equal(projection.desiredNextAction.action,'see_if_comparison_is_worthwhile');assert.equal(projection.engagementMode,'requesting_comparison');assert.equal(projection.exactCustomerWords[0],'My renewal increased');assert.equal(projection.numericScore,null);assert.equal(projection.buyingPrediction,null);assert.equal(projection.identityAutoMerged,false);assert.ok(projection.prohibitedInputs.includes('demographics'));const unified=projectUnifiedProducerRecord(checkpoint);assert.equal(unified.opportunity.projectionType,'producer_opportunity_nonnumeric');assert.equal(unified.ownership.silentlyReassigned,false);assert.equal(JSON.stringify(projection).includes('homeValue'),false);
console.log(JSON.stringify({sprint:'CF-PVX-OPP-2.0',pass:true,checks:14}));
