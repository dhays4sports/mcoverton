(function(root,factory){'use strict';const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;root.CoverageFitValueLearningLoop=api;})(typeof window!=='undefined'?window:globalThis,function(){'use strict';const VERSION='2.0.0',BUILD='CF-ADV-1.22',CONTRACT_ID='coveragefit-progressive-value-learning-v1',STAGES=Object.freeze(['snapshot_viewed','snapshot_saved','home_profile_started','home_profile_ready','contact_connected','policy_review_ready','quote_started','quote_delivered','sale_bound']),INFLUENCES=Object.freeze(['review_topic','topic_response','snapshot_saved','home_profile_value','policy_evidence','recommendation','recommendation_buy_in','producer_contact']);
  const clean=(value,max=120)=>String(value??'').trim().replace(/[<>\u0000-\u001f\u007f]/g,'').slice(0,max);
  function movement({journeyId,from,to,influences=[],occurredAt=new Date().toISOString()}={}){if(!STAGES.includes(from)||!STAGES.includes(to)||STAGES.indexOf(to)<=STAGES.indexOf(from))throw new TypeError('A forward value transition is required.');return{schemaVersion:'1.0',contractId:CONTRACT_ID,eventType:'value_movement',journeyId:clean(journeyId),from,to,influences:[...new Set(influences.filter(item=>INFLUENCES.includes(item)))],occurredAt,interpretation:'association_only',sensitiveContentIncluded:false,customerWordsIncluded:false,contactDetailsIncluded:false}}
  function summarize(events=[]){return{transitions:events.length,byDestination:Object.fromEntries(STAGES.map(stage=>[stage,events.filter(event=>event.to===stage).length])),influenceCounts:Object.fromEntries(INFLUENCES.map(key=>[key,events.filter(event=>event.influences.includes(key)).length])),causalClaims:false}}
  return Object.freeze({VERSION,BUILD,CONTRACT_ID,STAGES,INFLUENCES,movement,summarize});
});
