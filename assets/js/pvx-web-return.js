(function(root){'use strict';async function install(){const status=document.getElementById('pvxWebReturnStatus'),raw=new URLSearchParams(root.location.search).get('return')||'';root.history.replaceState({},'',root.location.pathname);if(!/^pvxwr_[A-Za-z0-9_-]{43}$/.test(raw)){status.textContent='This secure return is unavailable. Start a new Snapshot or use your latest saved report.';return;}status.textContent='Restoring your exact place…';try{const response=await fetch('/api/pvx/web-return',{method:'POST',credentials:'same-origin',cache:'no-store',headers:{'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify({action:'consume',token:raw})}),body=await response.json().catch(()=>null);if(!response.ok)throw new Error(body?.error?.message||'This secure return is unavailable.');root.location.replace(body.destination);}catch(error){status.textContent=error.message;}}root.addEventListener('DOMContentLoaded',install,{once:true});})(window);
