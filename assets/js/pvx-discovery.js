(function (root, factory) {
  'use strict'; const api = factory(root); if (typeof module === 'object' && module.exports) module.exports = api; root.CoverageFitPVXDiscovery = api;
})(typeof window !== 'undefined' ? window : globalThis, function (root) {
  'use strict';
  const VERSION='1.0.0', BUILD='CF-PVX-1.3', CONTRACT_ID='coveragefit-five-minute-home-discovery-v1', STORAGE_KEY='coveragefit_pvx_discovery_v1';
  const QUESTIONS=Object.freeze([
    {id:'shoppingReason',stage:'Your goals',kicker:'Why now?',title:'What’s got you shopping right now?',description:'Choose the closest answer.',help:'Your reason for looking keeps the Snapshot relevant to this moment.',type:'single',autoAdvance:true,options:[['renewal_increase','My renewal price changed'],['buying_home','I’m buying a home'],['service_change','I want a different service experience'],['life_change','Something changed in my life'],['comparison','I’m simply comparing'],['something_else','Something else']]},
    {id:'improvementPriorities',stage:'Your goals',kicker:'What would feel better?',title:'Besides price, anything you’d like to improve?',description:'Choose any that matter. “Only price” is valid.',help:'These are your priorities, not findings about your policy.',type:'multi',options:[['understanding','Understand what I have'],['claim_support','Feel better supported in a claim'],['agent_access','Have easier access to my agent'],['coordination','Coordinate my insurance better'],['price_only','Price is my only priority','exclusive'],['not_sure','Not sure yet','exclusive']]},
    {id:'ownershipDuration',stage:'Your home',kicker:'A little home context',title:'How long have you owned the house?',description:'A broad range is enough.',help:'Time in the home can make changes and rebuilding assumptions worth discussing later.',type:'single',autoAdvance:true,options:[['buying_now','I’m buying it now'],['under_1','Less than a year'],['1_4','1–4 years'],['5_9','5–9 years'],['10_plus','10 years or more'],['not_sure','Not sure']]},
    {id:'stayIntent',stage:'Your home',kicker:'Looking ahead',title:'Do you see yourself staying there?',description:'Choose what feels closest today.',help:'This helps distinguish a short-term comparison from long-term home planning.',type:'single',autoAdvance:true,options:[['long_term','Yes, for the long term'],['few_years','Probably a few more years'],['may_move','I may move soon'],['not_sure','Not sure yet']]},
    {id:'upgradeSummary',stage:'Your home',kicker:'What has changed?',title:'Any significant upgrades?',description:'No technical details needed yet.',help:'Updated homes can deserve a later look at reconstruction assumptions. This answer alone is not a policy finding.',type:'single',autoAdvance:true,options:[['yes_major','Yes, meaningful updates'],['some','A few smaller updates'],['none','No significant updates'],['not_sure','Not sure']]},
    {id:'otherProperties',stage:'Your home',kicker:'The bigger picture',title:'Any other properties?',description:'We only need the broad picture for now.',help:'Multiple properties can make account and liability coordination worth reviewing.',type:'single',autoAdvance:true,options:[['rental','Yes, a rental property'],['second_home','Yes, a second home'],['multiple','Yes, more than one other property'],['none','No'],['not_sure','Not sure']]},
    {id:'claimExperience',stage:'Your experience',kicker:'Past experience',title:'Have you had any claims before?',description:'A broad answer is enough before your Snapshot.',help:'Claim experience can shape what service, deductibles, or prevention topics feel most relevant.',type:'single',autoAdvance:true,options:[['yes_smooth','Yes, and it went smoothly'],['yes_difficult','Yes, and it was difficult'],['yes_neutral','Yes'],['none','No'],['prefer_not','Prefer not to say'],['not_sure','Not sure']]},
    {id:'permissionToAdvise',stage:'Your Snapshot',kicker:'How Dylan can help',title:'If Dylan sees something he would approach differently, are you open to seeing why?',description:'This is permission to explain—not permission to change coverage.',help:'Your answer guides the conversation. It is not recommendation buy-in or authorization to bind.',type:'single',autoAdvance:true,options:[['yes','Yes, show me why'],['simple','Maybe—keep it simple'],['cost_first','Only if cost stays central'],['not_sure','Not sure yet']]}
  ].map(question=>Object.freeze({...question,options:Object.freeze(question.options.map(option=>Object.freeze({value:option[0],label:option[1],exclusive:option[2]==='exclusive'})))})));
  const clone=value=>value==null?value:JSON.parse(JSON.stringify(value));
  const clean=(value,max=240)=>String(value??'').trim().replace(/[<>\u0000-\u001f\u007f]/g,'').slice(0,max);
  function initialState(){return{schemaVersion:'1.0',contractId:CONTRACT_ID,currentQuestionId:QUESTIONS[0].id,answers:{},exactCustomerWords:{},startedAt:new Date().toISOString(),updatedAt:new Date().toISOString(),completedAt:null};}
  function nextUnansweredQuestion(profile={},afterQuestionId=''){const prefilled=new Set(Array.isArray(profile.prefilledQuestionIds)?profile.prefilledQuestionIds:[]),start=Math.max(-1,QUESTIONS.findIndex(q=>q.id===afterQuestionId));for(let index=start+1;index<QUESTIONS.length;index+=1){const id=QUESTIONS[index].id;if(!(prefilled.has(id)&&profile.answers?.[id]!=null))return id}return'complete'}
  function validateCatalog(questions=QUESTIONS){const terms=/dwelling limit|liability limit|ordinance|replacement cost|coverage [a-z]|deductible percentage/i;return{valid:questions.length<=8&&questions.every(q=>q.options.some(o=>/not sure/i.test(o.label))||['shoppingReason','improvementPriorities'].includes(q.id))&&!questions.some(q=>terms.test(`${q.title} ${q.description}`)),count:questions.length,technicalTermsFound:questions.filter(q=>terms.test(`${q.title} ${q.description}`)).map(q=>q.id)};}
  function captureAnswer(profile,questionId,value,words){const question=QUESTIONS.find(item=>item.id===questionId);if(!question)throw new TypeError('Unknown discovery question.');const next={...initialState(),...clone(profile),answers:{...(profile?.answers||{})},exactCustomerWords:{...(profile?.exactCustomerWords||{})}};const allowed=new Set(question.options.map(o=>o.value));const values=Array.isArray(value)?value:[value];if(!values.length||values.some(item=>!allowed.has(item)))throw new TypeError('Unsupported discovery answer.');next.answers[questionId]=question.type==='multi'?[...new Set(values)]:values[0];const exact=clean(words);if(exact)next.exactCustomerWords[questionId]=exact;next.updatedAt=new Date().toISOString();return next;}
  function completeProfile(profile,at=new Date().toISOString()){const next={...initialState(),...clone(profile)};if(QUESTIONS.some(q=>next.answers[q.id]==null))throw new TypeError('All eight discovery questions are required; Not sure is valid.');next.currentQuestionId='complete';next.completedAt=at;next.updatedAt=at;return next;}
  function install(){const doc=root.document;if(!doc?.body?.hasAttribute('data-pvx-discovery'))return null;const $=id=>doc.getElementById(id);let state=(()=>{try{return JSON.parse(root.localStorage.getItem(STORAGE_KEY)||'null')||initialState();}catch(_){return initialState();}})();const prefilled=new Set(Array.isArray(state.prefilledQuestionIds)?state.prefilledQuestionIds:[]);const isCarried=i=>Boolean(QUESTIONS[i]&&prefilled.has(QUESTIONS[i].id)&&state.answers?.[QUESTIONS[i].id]!=null);const nextEditable=from=>{let next=from+1;while(next<QUESTIONS.length&&isCarried(next))next+=1;return next};const previousEditable=from=>{let previous=from-1;while(previous>=0&&isCarried(previous))previous-=1;return previous};let index=Math.max(0,QUESTIONS.findIndex(q=>q.id===state.currentQuestionId));if(isCarried(index))index=nextEditable(index-1);const save=()=>{state.currentQuestionId=QUESTIONS[index]?.id||'complete';state.updatedAt=new Date().toISOString();try{root.localStorage.setItem(STORAGE_KEY,JSON.stringify(state));}catch(_){}};const announce=m=>{$('pvxDiscoveryLive').textContent='';root.setTimeout(()=>{$('pvxDiscoveryLive').textContent=m;},20)};
    function render(){if(index>=QUESTIONS.length)return finish();const q=QUESTIONS[index],p=Math.round(((index+1)/QUESTIONS.length)*100);$('pvxDiscoveryQuestion').hidden=false;$('pvxDiscoveryListening').hidden=true;$('pvxDiscoveryComplete').hidden=true;$('pvxDiscoveryKicker').textContent=q.kicker;$('pvxDiscoveryTitle').textContent=q.title;$('pvxDiscoveryDescription').textContent=q.description;$('pvxDiscoveryHelp').textContent=q.help;$('pvxDiscoveryHelp').hidden=true;$('pvxDiscoveryHelpToggle').setAttribute('aria-expanded','false');$('pvxDiscoveryStage').textContent=q.stage;$('pvxDiscoveryProgressText').textContent=`${q.stage} · Your Snapshot is getting closer`;$('pvxDiscoveryProgress').setAttribute('aria-valuenow',String(p));$('pvxDiscoveryProgressBar').style.width=`${p}%`;$('pvxDiscoveryBack').hidden=previousEditable(index)<0;$('pvxDiscoveryError').hidden=true;const stored=state.answers[q.id];const needsWords=stored==='something_else';$('pvxDiscoveryContinue').hidden=q.autoAdvance&&!needsWords;$('pvxExactWordsField').hidden=!needsWords;$('pvxExactWords').value=state.exactCustomerWords[q.id]||'';$('pvxDiscoveryControl').innerHTML=q.options.map(o=>`<button class="pvx-choice${q.type==='multi'?' pvx-choice--multi':''}" type="button" ${q.type==='single'?`role="radio" aria-checked="${stored===o.value}"`:`aria-pressed="${Array.isArray(stored)&&stored.includes(o.value)}"`} data-value="${o.value}"><span class="pvx-choice__copy"><strong>${o.label}</strong></span><span class="pvx-choice__mark" aria-hidden="true">✓</span></button>`).join('');bind(q);announce(`${q.stage}. ${q.title}.`);$('pvxDiscoveryMain').focus({preventScroll:true});}
    function choose(q,button){const value=button.dataset.value;if(q.type==='multi'){const option=q.options.find(o=>o.value===value);let values=Array.isArray(state.answers[q.id])?[...state.answers[q.id]]:[];if(option.exclusive)values=values.includes(value)?[]:[value];else{values=values.filter(item=>!q.options.find(o=>o.value===item)?.exclusive);values=values.includes(value)?values.filter(item=>item!==value):[...values,value];}state=captureAnswer(state,q.id,values);$('pvxDiscoveryControl').querySelectorAll('.pvx-choice').forEach(item=>item.setAttribute('aria-pressed',String(values.includes(item.dataset.value))));save();}else{state=captureAnswer(state,q.id,value);save();$('pvxDiscoveryControl').querySelectorAll('.pvx-choice').forEach(item=>item.setAttribute('aria-checked',String(item===button)));if(value==='something_else'){$('pvxExactWordsField').hidden=false;$('pvxDiscoveryContinue').hidden=false;$('pvxExactWords').focus();}else if(q.autoAdvance)listenAndAdvance(q);} }
    function bind(q){$('pvxDiscoveryControl').querySelectorAll('.pvx-choice').forEach(button=>button.addEventListener('click',()=>choose(q,button)));}
    function listenAndAdvance(q){$('pvxDiscoveryQuestion').hidden=true;$('pvxDiscoveryListening').hidden=false;const reaction=root.CoverageFitPVXContextualBranching?.acknowledgment?.(state,q.id);$('pvxDiscoveryListeningText').textContent=reaction||'Got it. We’re keeping this in your own context.';root.setTimeout(()=>{const next=nextEditable(index);if(next>=QUESTIONS.length)return finish();index=next;save();render();},root.matchMedia?.('(prefers-reduced-motion: reduce)').matches?0:180);}
    function finish(){state=completeProfile(state);save();$('pvxDiscoveryQuestion').hidden=true;$('pvxDiscoveryListening').hidden=true;$('pvxDiscoveryComplete').hidden=false;$('pvxDiscoveryProgress').setAttribute('aria-valuenow','100');$('pvxDiscoveryProgressBar').style.width='100%';announce('Discovery complete. We heard what matters.');try{root.dispatchEvent(new CustomEvent('coveragefit:pvx-discovery-complete',{detail:{contractId:CONTRACT_ID,questionCount:8,technicalQuestions:0,scoreCreated:false,contactCollected:false}}));}catch(_){} }
    $('pvxDiscoveryForm').addEventListener('submit',event=>{event.preventDefault();const q=QUESTIONS[index],value=state.answers[q.id];if(value==null||(Array.isArray(value)&&!value.length)){$('pvxDiscoveryError').textContent='Choose an answer. “Not sure” is always okay.';$('pvxDiscoveryError').hidden=false;return;}const words=clean($('pvxExactWords').value);if(words)state=captureAnswer(state,q.id,value,words);save();listenAndAdvance(q);});$('pvxDiscoveryBack').addEventListener('click',()=>{const previous=previousEditable(index);if(previous>=0){index=previous;save();render();}});$('pvxDiscoveryHelpToggle').addEventListener('click',()=>{const open=$('pvxDiscoveryHelp').hidden;$('pvxDiscoveryHelp').hidden=!open;$('pvxDiscoveryHelpToggle').setAttribute('aria-expanded',String(open));});$('pvxDiscoverySave').addEventListener('click',()=>{save();announce('Progress saved on this device.');});render();return{getState:()=>clone(state),render};}
  if(root.document)root.addEventListener('DOMContentLoaded',install,{once:true});
  return Object.freeze({VERSION,BUILD,CONTRACT_ID,STORAGE_KEY,QUESTIONS,initialState,nextUnansweredQuestion,validateCatalog,captureAnswer,completeProfile,install});
});
