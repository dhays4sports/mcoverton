(function(root,factory){'use strict';const api=factory(root);if(typeof module==='object'&&module.exports)module.exports=api;root.CoverageFitPropertyPrefill=api;})(typeof window!=='undefined'?window:globalThis,function(root){'use strict';const VERSION='1.0.0',BUILD='CF-HOME-3.2',CONTRACT_ID='coveragefit-property-prefill-confirmation-v1';
  const LABELS=Object.freeze({yearBuilt:'the home was built in',livingArea:'approximately',stories:'the home has',construction:'the construction is',foundation:'the foundation is',roofType:'the roof type is'}),UNITS=Object.freeze({livingArea:'square feet',stories:'stories'});
  function queue(profile){const rows=[];for(const section of ['physicalCharacteristics','construction','foundation','roof'])for(const [key,fact] of Object.entries(profile?.[section]||{}))if(['property_source_reported','conflict_needs_confirmation'].includes(fact?.source))rows.push({section,key,...fact});return rows}
  function prompt(item){if(!item)return'';if(item.key==='yearBuilt')return`We found that the home was built in ${item.value}. Is that still correct?`;if(item.key==='livingArea')return`We found approximately ${Number(item.value).toLocaleString('en-US')} square feet. Does that look right?`;return`We found ${LABELS[item.key]||item.key} ${item.value}${UNITS[item.key]?` ${UNITS[item.key]}`:''}. Does that look right?`}
  function answer(profile,item,choice,changedValue,at=new Date().toISOString()){const contract=root.CoverageFitHomeProfileContract||require('./pvx-home-profile-contract.js');if(choice==='yes')return contract.confirmFact(profile,item.section,item.key,item.value,at);if(choice==='change')return contract.setFact(profile,item.section,item.key,changedValue,{source:'customer_reported',evidenceRef:`homeProfile.${item.section}.${item.key}`,reportedAt:at,updatedAt:at});if(choice==='not_sure')return contract.setFact(profile,item.section,item.key,item.value,{source:'unknown',evidenceRef:`propertySource.${item.key}`,reportedAt:item.reportedAt,updatedAt:at});throw new TypeError('Choose yes, change, or not sure.')}
  return Object.freeze({VERSION,BUILD,CONTRACT_ID,LABELS,queue,prompt,answer});
});
