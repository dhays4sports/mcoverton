(function(root,factory){'use strict';const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;root.CoverageFitPVXContextualBranching=api;})(typeof window!=='undefined'?window:globalThis,function(){
  'use strict';
  const VERSION='1.0.0',BUILD='CF-PVX-1.4',CONTRACT_ID='coveragefit-pvx-contextual-branching-v1',MAX_DEPTH=1;
  const BRANCHES=Object.freeze({
    updateAreas:Object.freeze({id:'updateAreas',sourceQuestions:['stayIntent','upgradeSummary'],depth:1,type:'multi',title:'Which areas did you update?',description:'Choose any that apply. Broad answers are enough.',options:['Roof','Plumbing','Electrical','HVAC','Kitchen','Bathrooms','Addition','Other','Not sure']}),
    claimType:Object.freeze({id:'claimType',sourceQuestions:['claimExperience'],depth:1,type:'single',title:'What kind of claim was it?',description:'A broad category is enough.',options:['Water','Fire or smoke','Weather','Theft','Liability','Something else','Prefer not to say','Not sure']}),
    preservation:Object.freeze({id:'preservation',sourceQuestions:['incumbentTenure'],depth:1,type:'multi',title:'What would you want to preserve?',description:'Choose what already works well today.',options:['Agent relationship','Claims service','Billing setup','Policy features','Nothing specific','Not sure']})
  });
  const clone=value=>value==null?value:JSON.parse(JSON.stringify(value));
  function deriveBranches(profile={},context={}){const a=profile.answers||{};const branches=[];if(['long_term','few_years'].includes(a.stayIntent)&&['yes_major','some'].includes(a.upgradeSummary))branches.push(BRANCHES.updateAreas);if(/^yes_/.test(a.claimExperience||''))branches.push(BRANCHES.claimType);if(Number(context.incumbentTenureYears)>=10)branches.push(BRANCHES.preservation);return branches.map(clone);}
  function acknowledgment(profile={},questionId){const a=profile.answers||{};if(questionId==='improvementPriorities'&&Array.isArray(a.improvementPriorities)&&a.improvementPriorities.includes('price_only'))return 'Got it. We’ll keep the price issue central.';if(questionId==='upgradeSummary'&&['long_term','few_years'].includes(a.stayIntent)&&['yes_major','some'].includes(a.upgradeSummary))return 'Helpful. Since you have updated the home and plan to stay, the rebuilding assumptions may be worth checking.';if(questionId==='claimExperience'&&a.claimExperience==='yes_difficult')return 'Thanks for sharing that. We’ll keep claim expectations and service visible.';if(questionId==='otherProperties'&&!['none','not_sure',undefined].includes(a.otherProperties))return 'Helpful. We’ll keep the bigger insurance picture together.';if(questionId==='permissionToAdvise'&&a.permissionToAdvise==='cost_first')return 'Understood. Dylan can explain the tradeoff while keeping cost central.';return ''}
  function reconcileBranchAnswers(previous={},activeBranches=[]){const allowed=new Set(activeBranches.map(item=>item.id));return Object.fromEntries(Object.entries(previous||{}).filter(([key])=>allowed.has(key)));}
  function validateBranch(branch){return Boolean(branch&&branch.depth<=MAX_DEPTH&&branch.sourceQuestions?.length&&branch.options?.length&&branch.options.some(option=>/not sure|prefer not/i.test(option)));}
  return Object.freeze({VERSION,BUILD,CONTRACT_ID,MAX_DEPTH,BRANCHES,deriveBranches,acknowledgment,reconcileBranchAnswers,validateBranch});
});
