(function(root,factory){'use strict';const api=factory(root);if(typeof module==='object'&&module.exports)module.exports=api;root.CoverageFitRecommendationBuyIn=api;})(typeof window!=='undefined'?window:globalThis,function(root){'use strict';const VERSION='1.0.0',BUILD='CF-ADV-1.13',CONTRACT_ID='coveragefit-recommendation-buy-in-v1',STATES=Object.freeze(['accepted_logic','needs_explanation','prefers_savings','undecided']);
  function eligible(recommendation){return recommendation?.status==='actual_recommendation'&&recommendation?.recommendation===true&&Array.isArray(recommendation?.evidenceRefs)&&recommendation.evidenceRefs.length>0&&['existing_recommendation_engine','licensed_producer_verified'].includes(recommendation.authority)}
  function capture(recommendation,state,exactWords='',at=new Date().toISOString()){if(!eligible(recommendation))throw new TypeError('Evidence-backed recommendation required.');if(!STATES.includes(state))throw new TypeError('Unsupported recommendation response.');return{schemaVersion:'1.0',contractId:CONTRACT_ID,recordType:'recommendationResponse',recommendationKey:String(recommendation.recommendationKey||'').slice(0,180),state,exactCustomerWords:String(exactWords||'').trim().slice(0,500),createdAt:at,updatedAt:at,topicResponse:false,coveragePreference:false,finalDecision:false,bindAuthorization:false}}
  function upsert(list=[],response){return[...list.filter(item=>item.recommendationKey!==response.recommendationKey),response]}
  function cardStates(){const cards=root.CoverageFitAdvisoryRecommendationCards;return cards?.REACTION_STATES?.map(item=>item.value)||STATES.slice()}
  return Object.freeze({VERSION,BUILD,CONTRACT_ID,STATES,eligible,capture,upsert,cardStates});
});
