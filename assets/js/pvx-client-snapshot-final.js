(function(root,factory){'use strict';const api=factory(root);if(typeof module==='object'&&module.exports)module.exports=api;root.CoverageFitClientSnapshotFinal=api;})(typeof window!=='undefined'?window:globalThis,function(root){'use strict';const VERSION='2.0.0',BUILD='CF-ADV-1.20',CONTRACT_ID='coveragefit-client-snapshot-v2',clone=value=>value==null?value:JSON.parse(JSON.stringify(value));
  function finalize(ledger,{producerVerified=false,recommendations=[],decisions=[],nextSteps=[],quoteSummary=null,finalizedAt=new Date().toISOString()}={}){if(!producerVerified)throw new TypeError('Producer verification is required before finalizing the client report.');if(!recommendations.every(item=>item.status==='actual_recommendation'&&item.recommendation===true))throw new TypeError('Only actual recommendations may appear in the final report.');const contents={title:'Your CoverageFit Review',summary:'Your home, coverage review, and agreed next steps in one living report.',recommendations:clone(recommendations).map(({internalNote,producerNotes,...item})=>item),decisions:clone(decisions).map(({internalNote,...item})=>item),nextSteps:clone(nextSteps),quoteSummary:quoteSummary?{status:quoteSummary.status||'prepared',presentedAt:quoteSummary.presentedAt||'',premium:quoteSummary.customerVisiblePremium??null}:null,producerReviewed:true,finalizedAt,disclosure:'Recommendations reflect available evidence and producer review. Coverage is not bound until separately authorized and confirmed.'};const ledgerApi=root.CoverageFitReportRevisionLedger||require('./pvx-report-revision-ledger.js');return ledgerApi.append(ledger,{revision:'Final',revisionId:`Final_${finalizedAt.replace(/\D/g,'')}`,createdAt:finalizedAt,createdBy:'producer',contents})}
  return Object.freeze({VERSION,BUILD,CONTRACT_ID,finalize});
});
