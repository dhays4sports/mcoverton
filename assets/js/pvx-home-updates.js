(function(root,factory){'use strict';const api=factory(root);if(typeof module==='object'&&module.exports)module.exports=api;root.CoverageFitHomeUpdates=api;})(typeof window!=='undefined'?window:globalThis,function(root){'use strict';const VERSION='1.0.0',BUILD='CF-HOME-3.4',CONTRACT_ID='coveragefit-home-updates-branching-v1';
  const AREAS=Object.freeze(['roof','plumbing','electrical','hvac','kitchen','bathrooms','addition','flooring_finishes','structural_changes','high_end_materials']);
  function entry(discovery={}){const answer=discovery?.answers?.upgradeSummary;return['yes_major','yes_some'].includes(answer)?{prompt:'You mentioned making meaningful improvements. Which areas were updated?',reusesEvidenceRef:'discovery.answers.upgradeSummary',options:AREAS}:null}
  function followUps(selected=[]){return [...new Set(selected)].filter(value=>AREAS.includes(value)).map(area=>({id:`update_${area}`,area,prompt:`What changed in the ${area.replace(/_/g,' ')}?`,type:'brief_detail',depth:1,notSureAllowed:true}))}
  function apply(profile,selected=[],details={},at=new Date().toISOString()){const contract=root.CoverageFitHomeProfileContract||require('./pvx-home-profile-contract.js'),areas=[...new Set(selected)].filter(value=>AREAS.includes(value));return contract.setFact(profile,'systemsAndUpdates','updates',{areas,details:Object.fromEntries(areas.map(area=>[area,String(details[area]||'').trim().slice(0,300)]))},{source:'customer_reported',evidenceRef:'discovery.answers.upgradeSummary',reportedAt:at,updatedAt:at})}
  return Object.freeze({VERSION,BUILD,CONTRACT_ID,AREAS,entry,followUps,apply});
});
