(function(root,factory){'use strict';const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;root.CoverageFitLiveNotes=api;})(typeof window!=='undefined'?window:globalThis,function(){'use strict';const VERSION='2.0.0',BUILD='CF-ADV-1.17',CONTRACT_ID='coveragefit-live-notes-v2',text=(value,max=2000)=>String(value??'').trim().replace(/[<>\u0000-\u001f\u007f]/g,'').slice(0,max);
  function create({exactCustomerWords={},topicResponses=[]}={}){const preCall=[...Object.entries(exactCustomerWords).map(([key,value],index)=>({noteId:`customer_word_${index+1}`,recordType:'exact_customer_wording',source:'customer',key,text:text(value,800),immutable:true})),...topicResponses.map((response,index)=>({noteId:`topic_reaction_${index+1}`,recordType:'pre_call_topic_reaction',source:'customer',topicKey:text(response.topicKey,160),state:response.state,exactCustomerWords:text(response.exactCustomerWords,800),immutable:true}))];return{schemaVersion:'2.0',contractId:CONTRACT_ID,preCall,live:[]}}
  function add(journal,{body,author='Producer',anchor='',createdAt=new Date().toISOString()}={}){const clean=text(body);if(!clean)throw new TypeError('A live note is required.');return{...journal,live:[...(journal.live||[]),{noteId:`live_${createdAt.replace(/\D/g,'')}_${(journal.live||[]).length+1}`,recordType:'producer_live_note',source:'producer',author:text(author,120),body:clean,anchor:text(anchor,160),createdAt,customerVisible:false}]}}
  return Object.freeze({VERSION,BUILD,CONTRACT_ID,create,add});
});
