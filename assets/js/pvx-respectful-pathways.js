(function(root,factory){'use strict';const api=factory(root);if(typeof module==='object'&&module.exports)module.exports=api;root.CoverageFitPVXRespectfulPathways=api;})(typeof window!=='undefined'?window:globalThis,function(root){
  'use strict';const valueOf=value=>value&&typeof value==='object'&&'value'in value?value.value:value;
  function derive({discovery={},readiness=null,currentRelationship={}}={}){const priorities=Array.isArray(discovery?.answers?.improvementPriorities)?discovery.answers.improvementPriorities:[],tenure=valueOf(currentRelationship.tenure),mustKeep=Array.isArray(currentRelationship.mustKeep)?currentRelationship.mustKeep.map(valueOf):[],priceOnly=priorities.length===1&&priorities[0]==='price_only',continuity=Boolean(tenure||mustKeep.includes('carrier_relationship')||mustKeep.includes('agent_relationship'));let kind='standard',customerNarrative='';if(readiness?.state==='exploring'){kind='exploring';customerNarrative='You can keep exploring at your own pace. Nothing here requires a contact request.';}else if(readiness?.state==='not_sure'){kind='not_sure';customerNarrative='Not sure is a complete answer. Every option remains open.';}else if(continuity){kind='continuity';customerNarrative='Your existing relationship matters. Dylan would want to preserve what is working while understanding whether anything needs attention.';}else if(priceOnly||readiness?.state==='price_dependent'){kind='cost_focused';customerNarrative='Price is the priority you named. A useful comparison should keep cost central and make any tradeoffs clear.';}return Object.freeze({kind,customerNarrative,createsTopic:false,createsRecommendation:false,affectsProtectionScore:false,negativeLabel:false});}
  function read(key){try{return JSON.parse(root.localStorage?.getItem?.(key)||'null')||{};}catch(_){return{};}}
  function render(){const record=read('coveragefit_pvx_readiness_v1'),current=[...(record.actionReadinessExpressions||[])].sort((a,b)=>String(a.expressedAt||'').localeCompare(String(b.expressedAt||''))).at(-1),pathway=derive({discovery:read('coveragefit_pvx_discovery_v1'),readiness:current?.status==='active'?current:null,currentRelationship:read('coveragefit_prospect_profile_v1').currentRelationship||{}}),output=root.document?.getElementById('pvxRespectfulPathway'),contact=root.document?.getElementById('pvxContactRequest');if(output){output.textContent=pathway.customerNarrative;output.hidden=!pathway.customerNarrative;}if(contact){contact.dataset.customerPace=pathway.kind;if(pathway.kind==='exploring')root.document.getElementById('pvxContactRequestTitle').textContent='Human help is here if you decide you want it.';}return pathway;}
  function install(){if(!root.document?.body?.hasAttribute('data-pvx-snapshot'))return;render();root.addEventListener('coveragefit:readiness-expressed',render);root.addEventListener('coveragefit:readiness-skipped',render);}
  root.addEventListener?.('DOMContentLoaded',install,{once:true});return Object.freeze({VERSION:'1.0.0',BUILD:'CF-PVX-READY-1.5',derive,render,install});
});
