(function(root){'use strict';function install(){const form=document.getElementById('pvxPolicyUpload');if(!form)return;const input=document.getElementById('pvxPolicyFiles'),status=document.getElementById('pvxPolicyStatus'),token=new URLSearchParams(root.location.search).get('token')||'';form.addEventListener('submit',async event=>{event.preventDefault();status.textContent='Uploading privately…';try{const result=await root.CoverageFitPolicyIntake.upload(token,input.files);status.textContent=`${result.documents.length} policy file${result.documents.length===1?'':'s'} received for private review.`}catch(error){status.textContent=error.message;}})}root.addEventListener('DOMContentLoaded',install,{once:true});})(window);
