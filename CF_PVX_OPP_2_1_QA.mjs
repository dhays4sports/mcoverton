import assert from 'node:assert/strict';import fs from 'node:fs';import {buildConversationReadyBrief} from './server/pvx-producer-brief-core.mjs';
if(process.env.COVERAGEFIT_REGRESSION!=='1')assert.equal(fs.readFileSync('VERSION','utf8').trim(),'3.20.185');
const record={shoppingMotivation:{label:'Your renewal changed'},snapshotStatus:'saved',homeProfileStatus:'not_started',policyReviewStatus:'not_started',exactCustomerWords:[{words:'My renewal jumped again'}],reviewTopics:[{topicKey:'cost',status:'worth_reviewing',recommendation:false}],opportunity:{whyNow:{headline:'Your renewal changed'},desiredImprovement:[{label:'Keep price central'}],whatCustomerLearned:[{label:'Cost-focused comparison'}],currentActionReadiness:{state:'price_dependent'},currentChangeScope:'unknown',desiredNextAction:{action:'see_if_comparison_is_worthwhile'},topicResponses:[],contactPlan:{preferredMethod:'call',bestTime:'Tuesday afternoon',purpose:'comparison'}}};
const brief=buildConversationReadyBrief(record);assert.equal(brief.readiness,'Would consider action only if price improves');assert.equal(brief.changeScope,'Unknown');assert.equal(brief.desiredNextAction,'Decide whether a comparison is worthwhile');assert.match(brief.suggestedOpener,/You mentioned “My renewal jumped again.”/);assert.match(brief.recommendedNextQuestion,/comparison useful/);assert.deepEqual(brief.conversationSequence,['Listen','Connect','Recommend','Ask']);assert.equal(brief.actualRecommendations.length,0);assert.equal(brief.coveragePitch,null);assert.equal(brief.eligibilityConclusion,null);assert.equal('internalNotes'in brief,true);assert.equal(brief.internalNotes,undefined);assert.deepEqual(brief.completedCheckpoints,['Snapshot']);assert.deepEqual(brief.missingCheckpoints,['Home Profile','Current Policy Review']);
const workspace=fs.readFileSync('agent/workspace/index.html','utf8');assert.ok(workspace.includes('pvxProducerBriefs'));const ui=fs.readFileSync('assets/js/pvx-unified-workspace.js','utf8');assert.ok(ui.includes('textContent'));assert.ok(!ui.includes('innerHTML'));
console.log(JSON.stringify({sprint:'CF-PVX-OPP-2.1',pass:true,checks:16}));
