# CF-PVX-SMS-1.1 Release Certification

Certified when the focused adapter suite, full regression comparison, protected-hash comparison, root deployment check, and extracted-package rerun all pass.

The adapter preserves legacy `/sms/continue/` links, consumes valid handoffs once, creates one PVX journey, issues a separate secure resume credential, and contains no legacy `/home/` destination.
