# 408-CF-PVX-WEB-1.1 Regression Comparison

CoverageFit: 240 discovered, 152 passing, 88 historical failures. The historical failure set is unchanged from v3.20.135 and both native-web focused suites pass. The deployment harness was extended to recognize the intentionally private, noindex `/pvx/web/` bootstrap utility without adding it to the public sitemap.

408FARMERS: the raw historical suite remains dominated by stale exact-version, missing external dependency, visual-freeze, and cross-fixture failures. Both new web convergence suites pass and all protected hashes remain unchanged. No new non-version product failure remains.

