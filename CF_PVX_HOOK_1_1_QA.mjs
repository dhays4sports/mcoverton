import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import{createRequire}from'node:module';const require=createRequire(import.meta.url);assert.ok(Number(fs.readFileSync('VERSION','utf8').trim().split('.').at(-1))>=166);const root=process.env.FARMERS408_ROOT||path.resolve('../408farmers'),api=require(path.join(root,'shared/pvx-native-entry.js'));assert.match(api.BUILD,/^408-CF-PVX-HOOK-1\.[1-3]$/);assert.match(api.ROUTES.homepage.intro,/before any contact request/);assert.equal(api.ROUTES.home.choices.some(item=>item[0]==='renter'),true);assert.equal(api.ROUTES.home.choices.some(item=>item[0]==='contact_dylan'),true);assert.equal(api.fields('review_home_auto',{route_path:'/auto-bundle/',entry_type:'home_auto'},{bootstrapId:'pvxb_abcdefghijklmnop'}).bundle_context,'home_auto');console.log('408-CF-PVX-HOOK-1.1 QA: PASS');
