import assert from 'node:assert/strict';import fs from 'node:fs';import {nextUsefulUnlock} from './server/pvx-next-unlock-core.mjs';
assert.ok(Number(fs.readFileSync(new URL('./VERSION',import.meta.url),'utf8').trim().split('.').at(-1))>=159);let next=nextUsefulUnlock({journeyId:'j1'});assert.equal(next.primary.path,'home_profile');assert.equal(next.primary.required,false);assert.equal(next.alternatives.some(item=>item.path==='current_policy'),true);next=nextUsefulUnlock({journeyId:'j2',explicitContinuationChoice:'policy_first'});assert.equal(next.primary.path,'current_policy');next=nextUsefulUnlock({journeyId:'j3',homeProfilePath:{status:'complete'}});assert.equal(next.primary.path,'current_policy');next=nextUsefulUnlock({journeyId:'j4',homeProfilePath:{status:'complete'},currentPolicyPath:{status:'complete'}});assert.equal(next.primary.path,'combined');next=nextUsefulUnlock({journeyId:'j5',resumeState:{status:'paused'}});assert.equal(next.primary.action,'continue_later');for(const result of [nextUsefulUnlock({rush:true,costPriority:true,producerStatus:'urgent'}),next]){assert.equal(result.guardrails.urgencyManufactured,false);assert.equal(result.guardrails.savingRequired,false);assert.equal(result.guardrails.contactRequired,false)}console.log('CF-PVX-INSIGHT-1.7 QA: PASS');
